#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main()
{
	int num = 0;
	int result = 0;
	printf("请输入任意一个自然数，系统将判断其奇偶：");
	scanf("%d", &num);
	result = num % 2;
	if (result == 0)
		printf("这个数是一个偶数\n");
	else
		printf("这个数是一个奇数\n");
    return 0;
}
